apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocb-auth.fullname" . }}
  labels:
    {{- include "ocb-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocb-auth.selectorLabels" . | nindent 4 }}